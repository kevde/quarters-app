- name: allowed-queries
  definition:
    queries:
      - name: getBeaches
        query: |
          query GetBeaches {
            beaches {
              beach
              id
              link
              town
            }
          }
